

const initialState = {
  contacts: null,
  loading: false,
  error: null,
  show: false,
};

const contactsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default contactsReducer;